NOT = "~" # if you want space between "not" sign and variable, put it here, like "not " instead of "not"
AND = " ^ "
OR = " v "
ONE = '1'
ZERO = '0'

CSV_PATH = 'table.csv'
MODEL_PATH = 'example_model.yml'

ONEZERO = {
    True: ONE,
    False: ZERO
}